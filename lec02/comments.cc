#include "../common.h"

/* 
  This is how to insert comments.
  Everything between "slash+star" and "star+slash" 
  symbols is regarded as comments!
*/

int comments() {
	// This function does nothing but returning.

	// Your comment may start with either two slashes
	/// or three slashes; but the latter is often for documentation (e.g., doxygen)
	return 0;
}

